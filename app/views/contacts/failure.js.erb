<% message = "Computer says no." %>
<% joke = ["You've used one of these before, right?", "It's not that hard.", "C'mon now.", ""].sample %>
<% submessage = "#{contact.errors.full_messages.to_sentence}. #{joke}" %>

$('.alert').remove();
$('.content').prepend("<%= j render('layouts/flash', type: :error, message: message, submessage: submessage) %>")
